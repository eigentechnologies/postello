import React from 'react';

import './busy.sass';

function Busy() {
  return(
    <div className="busy-sign">
      <div className="busy-img" style={{backgroundImage: `url(${x})`}}/>>
    </div>
  )
}

export default Busy;